@model IEnumerable<Boletim.ATIVIDADE_PROFMT>

@{
    ViewBag.Title = "Index";
}
<br />
<h2>Listagem de Atividade</h2>
<br />
<p>
    @Html.ActionLink("Adicionar Atividade", "Create", new { }, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            NOME DA ATIVIDADE
        </th>
        <th>
            NOME DA MATERIA
        </th>
        <th>
            TURMA
        </th>
        <th>
            PERIODO
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ATIVIDADE.NOME_ATIVIDADE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MATERIA.NOME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TURMA.SERIE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TURMA.PERIODO_LET)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }, new { @class = "btn btn-warning"  }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }, new { @class = "btn btn-primary" }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>

    @Html.ActionLink("Voltar", "Listagem", "PROFMATERIATURMA", new {@class= "glyphicon glyphicon-pencil" }, new { @class = "btn btn-primary " })
     